# WordPress Helm Chart - Example Configuration Template
#
# 📄 이 파일의 역할:
# - Git에 커밋되는 예시/템플릿 파일
# - 민감한 정보 없이 설정 형식과 권장값 제공
# - 새로운 환경 구축 시 참고용
#
# 🔒 실제 사용 방법:
# 1. 이 파일을 values.yaml로 복사: cp .values.yaml values.yaml
# 2. values.yaml에서 민감정보 입력 (비밀번호, 도메인, 키 등)
# 3. values.yaml은 .gitignore에 포함되어 Git 추적 안 됨
#
# ⚠️ 주의:
# - .values.yaml: 예시 파일 (Git 커밋 ✅, 민감정보 ❌)
# - values.yaml: 실제 설정 (Git 커밋 ❌, 민감정보 ✅)

global:
  storageClass: "local-path"

# WordPress configuration
wordpress:
  image:
    repository: wordpress
    tag: "6.8.3-php8.4-fpm"  # PHP-FPM with Nginx sidecar
    pullPolicy: IfNotPresent

  replicaCount: 1

  # Nginx configuration (sidecar container)
  nginx:
    version: "1.25-alpine"
    maxUploadSize: "64M"
    fastcgiCache:
      enabled: false  # Enable for high-traffic sites

  service:
    type: ClusterIP
    port: 80

  ingress:
    enabled: true
    className: "nginx"
    annotations:
      cert-manager.io/cluster-issuer: "letsencrypt-prod"
      nginx.ingress.kubernetes.io/ssl-redirect: "true"
      nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
      # Rate limiting (per IP address)
      # Recommended for production: 100 req/sec with 5x burst (500 total)
      # Adjust based on your traffic patterns
      nginx.ingress.kubernetes.io/limit-rps: "100"
      nginx.ingress.kubernetes.io/limit-burst-multiplier: "5"
      # For high-traffic sites, consider: "200" rps with "10" burst multiplier
    hosts:
      - host: example.com  # 🔒 실제 도메인으로 변경 필요
        paths:
          - path: /
            pathType: Prefix
      - host: www.example.com  # 🔒 실제 도메인으로 변경 필요
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: example-com-tls  # 도메인에 맞게 변경 (예: dyibs-com-tls)
        hosts:
          - example.com  # 🔒 실제 도메인으로 변경 필요
          - www.example.com  # 🔒 실제 도메인으로 변경 필요

  resources:
    limits:
      cpu: 1000m
      memory: 1Gi
    requests:
      cpu: 250m
      memory: 512Mi

  persistence:
    enabled: true
    existingClaim: ""
    storageClass: "local-path"
    accessMode: ReadWriteOnce
    size: 500Mi

  # NFS storage for wp-content (optional)
  nfs:
    enabled: false  # Set to true if using NFS
    server: "your-nas-server.local"  # 🔒 실제 NAS 서버 주소로 변경 (예: nas.example.com)
    path: "/path/to/wordpress"  # 🔒 실제 NFS 경로로 변경 (예: /volume1/wordpress)
    size: "2Gi"
    mountOptions:
      - nfsvers=4.1
      - rsize=1048576
      - wsize=1048576
      - hard
      - timeo=600
      - retrans=2
    # Paths to preserve on NFS
    preservePaths:
      wpContent: true      # Mount wp-content from NFS
      wpConfig: true       # Mount wp-config.php from NFS

  # WordPress environment configuration
  wordpressUsername: admin  # 🔒 WordPress 관리자 계정명 (예: admin, yourname)
  wordpressPassword: ""  # 🔒 REQUIRED: 강력한 비밀번호 설정 필수
  wordpressEmail: admin@example.com  # 🔒 실제 이메일 주소로 변경
  wordpressBlogName: "My WordPress Site"  # 🔒 사이트 이름 (예: "회사명", "블로그명")

  # Skip initial installation if data exists
  wordpressSkipInstall: "no"

  # Database configuration
  database:
    host: "{{ .Release.Name }}-mariadb"  # 기본값: Helm release 이름 기반
    port: 3306
    name: wordpress  # 데이터베이스 이름 (일반적으로 변경 불필요)
    user: wordpress  # 🔒 데이터베이스 사용자명 (권장: 프로젝트명 등)

  # WordPress configuration (wp-config.php managed by ConfigMap)
  tablePrefix: "wp_"
  debug: false

  # WordPress security keys - 🔒 REQUIRED (보안을 위해 반드시 설정)
  # 생성 방법: https://api.wordpress.org/secret-key/1.1/salt/ 에서 자동 생성
  # ⚠️ 각 환경마다 고유한 키를 사용해야 합니다 (절대 예시값 사용 금지)
  authKey: ""  # 🔒 64자 랜덤 문자열
  secureAuthKey: ""  # 🔒 64자 랜덤 문자열
  loggedInKey: ""  # 🔒 64자 랜덤 문자열
  nonceKey: ""  # 🔒 64자 랜덤 문자열
  authSalt: ""  # 🔒 64자 랜덤 문자열
  secureAuthSalt: ""  # 🔒 64자 랜덤 문자열
  loggedInSalt: ""  # 🔒 64자 랜덤 문자열
  nonceSalt: ""  # 🔒 64자 랜덤 문자열

  # Extra wp-config.php configuration (optional)
  extraConfig: |
    // HTTPS detection for reverse proxy (Nginx Ingress)
    if (isset($_SERVER['HTTP_X_FORWARDED_PROTO']) && $_SERVER['HTTP_X_FORWARDED_PROTO'] === 'https') {
        $_SERVER['HTTPS'] = 'on';
    }

    // Force HTTPS for admin
    define('FORCE_SSL_ADMIN', true);
    define('WP_MEMORY_LIMIT', '256M');

# MariaDB configuration
mariadb:
  enabled: true

  image:
    repository: mariadb
    tag: "11.2"
    pullPolicy: IfNotPresent

  auth:
    rootPassword: ""  # 🔒 REQUIRED: MariaDB root 비밀번호 (강력한 비밀번호 필수)
    database: wordpress  # 데이터베이스 이름 (WordPress database.name과 동일해야 함)
    username: wordpress  # 🔒 데이터베이스 사용자명 (WordPress database.user와 동일해야 함)
    password: ""  # 🔒 REQUIRED: 데이터베이스 비밀번호 (강력한 비밀번호 필수)
    existingSecret: ""  # 기존 Secret 사용 시 이름 지정 (선택사항)

  primary:
    service:
      type: ClusterIP
      port: 3306

    persistence:
      enabled: true
      existingClaim: ""
      storageClass: "local-path"
      accessMode: ReadWriteOnce
      size: 500Mi

    resources:
      limits:
        cpu: 1000m
        memory: 1Gi
      requests:
        cpu: 250m
        memory: 512Mi

    # MariaDB configuration (optional - disabled by default for official image)
    configuration: ""
    # Uncomment below for custom configuration:
    # configuration: |-
    #   [mysqld]
    #   max_allowed_packet=16M
    #   character-set-server=utf8mb4
    #   collation-server=utf8mb4_general_ci

# Global settings


# Common labels
commonLabels:
  app.kubernetes.io/managed-by: "Helm"

# Common annotations
commonAnnotations: {}

# Cert-manager ClusterIssuer (should be created separately)
certManager:
  enabled: true
  issuer:
    name: letsencrypt-prod  # ClusterIssuer 이름 (일반적으로 변경 불필요)
    email: admin@example.com  # 🔒 실제 이메일로 변경 (Let's Encrypt 알림용)
    server: https://acme-v02.api.letsencrypt.org/directory  # Production 서버
